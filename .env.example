# Copy to .env and fill in. git-ignored: never commit the real key.
# Searched as <cwd>/.env by default; override with IRMINSUL_ENV_FILE=/abs/path
# (must be set in the shell — not inside this file — before irminsul runs).
VOYAGE_API_KEY=pa-xxxxxxxxxxxxxxxxxxxxxxxx
