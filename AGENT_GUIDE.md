# AGENT_GUIDE

Phase 5 deliverable — the paste-in syntax block an integrator drops into an
agent's system context (describing irminsul-io v1: `irminsul search "..." --limit 5 --json`,
exit codes, stdout/stderr rules, and the pinned interaction example).

Until then, the machine-readable contract already lives at `irminsul help --json` and
`irminsul schema <cmd> --json` (both rootless).
